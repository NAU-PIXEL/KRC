# Constant vs. cos(i)-scaled absorption length

`Check_effectcosi_scaleabs.sh` runs both treatments; `ploteffectcosi.py` plots
`Effect_incidenceangle.png` (surface temperature) and
`Effect_incidenceangle_RelError.png` (relative error between the two).
