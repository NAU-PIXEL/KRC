# Refractive index construction

`ScriptComputeSynthetic_refractiveindex.py` builds the synthetic n(k), n(r)
spectra (`Syntheticsppectrum_*.txt`) from digitized lab data (`Curtis_*`,
`Hudgins_*`), validated against `FerrariData_n*.txt`
(`Spectrum_*_validationwithFer.png`).
