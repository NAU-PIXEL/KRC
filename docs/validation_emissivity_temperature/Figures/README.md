# Fit-quality figures

`Emis_a_*.png` / `Emis_c_*.png` -- fitted vs. true emissivity for varying
absorption coefficient (`a`) and grain-size parameter (`c`).
`Figure_EmissIcevsT.pdf` -- summary figure (all cases).
`True_emiss_*.tab` / `Fit_emiss_*.tab` / `Temp.tab` -- the underlying data,
plotted by `plotfit.py`.
