# Fit data and figures

`PlotCurve.py` plots true vs. fitted conductivity (`Truecond_*`, `Fitcond_*`,
`newFitcond_*`) for the WBB and GJRKBB mixing models, amorphous ("a") and
crystalline ("c") ice, at several porosities/grain radii. `Figures/` holds the
rendered comparisons.
