# Generic conductivity code

`ComputeConductivity.dvrc` / `ComputeK.dvrc` -- reusable conductivity
computation, factored out for use by both the reproduction scripts and the
KRC-coefficient fit. See `README` inside.
